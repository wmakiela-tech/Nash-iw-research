[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "nash-iw-infrastructure"
version = "0.1.0"
description = "Validators and reproducibility support for NASH/IW"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
